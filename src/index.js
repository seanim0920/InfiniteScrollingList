import React from 'react';
import ReactDOM from 'react-dom';
import App from './APIList.js';

ReactDOM.render(<App />, document.getElementById('root')); //swaps out the root div with our own custom component